# 命令系统

## 1. 命令路由架构

### 1.1 主入口 (cli.ts)

```mermaid
flowchart TD
    A[main] --> B{命令参数}

    B -->|无参数| C[showBanner]
    B -->|find/search| D[runFind]
    B -->|init| E[runInit]
    B -->|add/a/install/i| F[runAdd]
    B -->|remove/rm/r| G[removeCommand]
    B -->|list/ls| H[runList]
    B -->|check| I[runCheck]
    B -->|update/upgrade| J[runUpdate]
    B -->|experimental_install| K[runInstallFromLock]
    B -->|experimental_sync| L[runSync]

    C --> Z[退出]
    D --> Z
    E --> Z
    F --> Z
    G --> Z
    H --> Z
    I --> Z
    J --> Z
    K --> Z
    L --> Z
```

### 1.2 命令映射表

| 命令 | 别名 | 处理函数 | 描述 |
|------|------|----------|------|
| `add` | `a`, `install`, `i` | `runAdd` | 安装技能 |
| `remove` | `rm`, `r` | `removeCommand` | 移除技能 |
| `list` | `ls` | `runList` | 列出已安装技能 |
| `find` | `search`, `f`, `s` | `runFind` | 搜索技能 |
| `check` | - | `runCheck` | 检查更新 |
| `update` | `upgrade` | `runUpdate` | 更新技能 |
| `init` | - | `runInit` | 创建技能模板 |
| `experimental_install` | - | `runInstallFromLock` | 从锁文件恢复 |
| `experimental_sync` | - | `runSync` | 从 node_modules 同步 |

## 2. 添加命令 (add.ts)

### 2.1 执行流程

```mermaid
sequenceDiagram
    participant User
    participant CLI
    participant Parser
    participant Git
    participant Discover
    participant Provider
    participant Installer
    participant Lock

    User->>CLI: skills add <source> [options]
    CLI->>Parser: parseAddOptions(args)
    Parser-->>CLI: { source, options }

    CLI->>Parser: parseSource(source)
    Parser-->>CLI: ParsedSource

    alt 检测私有仓库
        CLI->>Parser: isRepoPrivate()
        Parser-->>CLI: boolean
        CLI->>User: 显示警告（私有仓库）
    end

    CLI->>Git: cloneRepo(url)
    Git-->>CLI: tempDir

    CLI->>Discover: discoverSkills(tempDir)
    Discover-->>CLI: Skill[]

    CLI->>Provider: 注册远程提供者
    Provider-->>CLI: RemoteSkill[]

    alt 交互模式
        CLI->>User: 显示技能列表
        User->>CLI: 选择技能
    else 命令行指定
        CLI->>CLI: filterSkills()
    end

    CLI->>User: 选择目标代理
    User->>CLI: AgentType[]

    loop 每个技能
        loop 每个代理
            CLI->>Installer: installSkillForAgent()
            Installer-->>CLI: InstallResult
        end
    end

    CLI->>Lock: addSkillToLock()
    Lock-->>CLI: 确认

    CLI->>User: 显示安装结果
```

### 2.2 选项解析

```typescript
export interface AddOptions {
  global?: boolean;      // -g, --global
  agent?: string[];      // -a, --agent
  skill?: string[];      // -s, --skill
  list?: boolean;        // -l, --list
  yes?: boolean;         // -y, --yes
  copy?: boolean;        // --copy
  all?: boolean;         // --all
  fullDepth?: boolean;   // --full-depth
}
```

### 2.3 私有仓库检测

```typescript
async function isSourcePrivate(source: string): Promise<boolean | null> {
  const ownerRepo = parseOwnerRepo(source);
  if (!ownerRepo) return false;
  return isRepoPrivate(ownerRepo.owner, ownerRepo.repo);
}
```

**流程**：
1. 解析 owner/repo
2. 调用 GitHub API 检查 private 字段
3. 显示警告提示

## 3. 移除命令 (remove.ts)

### 3.1 移除流程

```mermaid
flowchart TD
    A[removeCommand] --> B[扫描已安装技能]
    B --> C[构建技能列表]

    C --> D{指定技能?}
    D -->|是| E[过滤匹配技能]
    D -->|否| F[显示交互选择]

    E --> G{找到技能?}
    G -->|否| H[显示错误]
    G -->|是| I[确认移除]

    F --> I
    I --> J{确认?}

    J -->|否| K[取消操作]
    J -->|是| L[执行移除]

    L --> M[遍历目标代理]
    M --> N[删除代理目录中的技能]

    N --> O{其他代理使用?}
    O -->|是| P[保留规范位置]
    O -->|否| Q[删除规范位置]

    P --> R[更新锁文件]
    Q --> R
    R --> S[显示结果]
```

### 3.2 幽灵符号链接清理

```mermaid
graph LR
    A[.agents/skills/my-skill] -->|有效| B[保留]
    A -->|幽灵链接| C[删除]

    D[.claude/skills/my-skill] -->|指向 A| E[检查 A]
    E -->|A 存在| B
    E -->|A 不存在| C
```

### 3.3 安全检查

```typescript
// 扫描所有可能的路径
for (const agentKey of targetAgents) {
  const agent = agents[agentKey];
  const pathsToCleanup = new Set([skillPath]);

  // 添加代理原生路径
  if (isGlobal && agent.globalSkillsDir) {
    pathsToCleanup.add(join(agent.globalSkillsDir, sanitizedName));
  } else {
    pathsToCleanup.add(join(cwd, agent.skillsDir, sanitizedName));
  }

  // 清理除规范路径外的所有路径
  for (const pathToCleanup of pathsToCleanup) {
    if (pathToCleanup !== canonicalPath) {
      await rm(pathToCleanup, { recursive: true, force: true });
    }
  }
}
```

## 4. 列表命令 (list.ts)

### 4.1 列表流程

```mermaid
flowchart TD
    A[runList] --> B[parseListOptions]
    B --> C{global?}

    C -->|true| D[扫描全局目录]
    C -->|false| E[扫描项目目录]
    C -->|未指定| F[扫描两者]

    D --> G{agentFilter?}
    E --> G
    F --> G

    G -->|有过滤器| H[过滤代理]
    G -->|无过滤器| I[检测已安装代理]

    H --> J[listInstalledSkills]
    I --> J

    J --> K[获取锁文件]
    K --> L[按插件分组]

    L --> M{有分组?}
    M -->|是| N[显示分组列表]
    M -->|否| O[显示扁平列表]

    N --> P[格式化输出]
    O --> P
```

### 4.2 显示格式

```bash
# 扁平列表
React Best Practices
  .agents/skills/react-best-practices
  Agents: Claude Code, Cursor, Codex

# 分组列表
Vercel AI SDK
  vercel-ai-sdk-chat .agents/skills/vercel-ai-sdk-chat
    Agents: Claude Code, Cursor
  vercel-ai-sdk-stream .agents/skills/vercel-ai-sdk-stream
    Agents: Claude Code
```

### 4.3 路径缩短

```typescript
function shortenPath(fullPath: string, cwd: string): string {
  const home = homedir();
  if (fullPath.startsWith(home)) {
    return fullPath.replace(home, '~');
  }
  if (fullPath.startsWith(cwd)) {
    return '.' + fullPath.slice(cwd.length);
  }
  return fullPath;
}
```

## 5. 查找命令 (find.ts)

### 5.1 查找模式

```mermaid
stateDiagram-v2
    [*] --> 检查模式
    检查模式 --> 交互模式: stdin.isTTY
    检查模式 --> 非交互模式: !stdin.isTTY

    非交互模式 --> API搜索
    API搜索 --> 显示结果
    显示结果 --> [*]

    交互模式 --> 显示搜索界面
    显示搜索界面 --> 等待输入

    等待输入 --> 输入处理
    输入处理 --> 防抖计时器
    防抖计时器 --> API搜索: 延迟后
    API搜索 --> 更新界面
    更新界面 --> 等待输入

    等待输入 --> 用户选择: Enter
    用户选择 --> 执行安装
    执行安装 --> [*]

    等待输入 --> 用户取消: Esc
    用户取消 --> [*]
```

### 5.2 实时搜索

```typescript
function triggerSearch(q: string): void {
  // 清除待处理的计时器
  if (debounceTimer) {
    clearTimeout(debounceTimer);
  }

  if (!q || q.length < 2) {
    results = [];
    selectedIndex = 0;
    render();
    return;
  }

  loading = true;
  render();

  // 自适应防抖：根据查询长度调整延迟
  const debounceMs = Math.max(150, 350 - q.length * 50);

  debounceTimer = setTimeout(async () => {
    results = await searchSkillsAPI(q);
    selectedIndex = 0;
    loading = false;
    render();
  }, debounceMs);
}
```

### 5.3 键盘处理

```mermaid
graph LR
    A[键盘输入] --> B{按键类型}

    B -->|Escape| C[取消]
    B -->|Enter| D[选择]
    B -->|Up| E[上移]
    B -->|Down| F[下移]
    B -->|Backspace| G[删除字符]
    B -->|字符| H[添加字符]

    C --> I[cleanup]
    D --> I
    E --> J[render]
    F --> J
    G --> K[triggerSearch]
    H --> K

    K --> J
    I --> L[返回结果]
```

### 5.4 API 搜索

```typescript
async function searchSkillsAPI(query: string): Promise<SearchSkill[]> {
  const url = `${SEARCH_API_BASE}/api/search?q=${encodeURIComponent(query)}&limit=10`;
  const res = await fetch(url);

  if (!res.ok) return [];

  const data = await res.json();
  return data.skills.map(skill => ({
    name: skill.name,
    slug: skill.id,
    source: skill.source || '',
    installs: skill.installs,
  }));
}
```

## 6. 检查更新 (check)

### 6.1 检查流程

```mermaid
sequenceDiagram
    participant User
    participant CLI
    participant Lock
    participant GitHub

    User->>CLI: skills check
    CLI->>Lock: readSkillLock()
    Lock-->>CLI: SkillLockFile

    CLI->>CLI: 按源分组技能

    loop 每个源
        CLI->>GitHub: fetchSkillFolderHash()
        GitHub-->>CLI: tree SHA

        alt 哈希不同
            CLI->>CLI: 添加到更新列表
        else 哈希相同
            CLI->>CLI: 跳过
        end
    end

    CLI->>User: 显示更新列表
```

### 6.2 跳过原因

```typescript
function getSkipReason(entry: SkillLockEntry): string {
  if (entry.sourceType === 'local') {
    return 'Local path';
  }
  if (entry.sourceType === 'git') {
    return 'Git URL (hash tracking not supported)';
  }
  if (!entry.skillFolderHash) {
    return 'No version hash available';
  }
  if (!entry.skillPath) {
    return 'No skill path recorded';
  }
  return 'No version tracking';
}
```

## 7. 更新命令 (update)

### 7.1 更新流程

```mermaid
flowchart TD
    A[runUpdate] --> B[读取锁文件]
    B --> C[检查更新]

    C --> D{有更新?}
    D -->|否| E[显示已是最新]
    D -->|是| F[构建更新列表]

    F --> G[循环更新]
    G --> H[重新安装技能]

    H --> I{成功?}
    I -->|是| J[成功计数++]
    I -->|否| K[失败计数++]

    J --> L{更多更新?}
    K --> L
    L -->|是| G
    L -->|否| M[显示结果]
```

### 7.2 重新安装策略

```typescript
// 构建安装 URL
let installUrl = update.entry.sourceUrl;
if (update.entry.skillPath) {
  // 转换为 tree URL
  installUrl = `${installUrl}/tree/main/${skillFolder}`;
}

// 调用 skills CLI 重新安装
const result = spawnSync('npx', ['-y', 'skills', 'add', installUrl, '-g', '-y'], {
  stdio: ['inherit', 'pipe', 'pipe'],
  shell: process.platform === 'win32',
});
```

## 8. 初始化命令 (init)

### 8.1 初始化流程

```mermaid
flowchart TD
    A[runInit] --> B[确定技能名称]
    B --> C{指定名称?}

    C -->|是| D[使用指定名称]
    C -->|否| E[使用当前目录名]

    D --> F[构建路径]
    E --> F

    F --> G{SKILL.md 存在?}
    G -->|是| H[显示已存在]
    G -->|否| I[创建目录]

    I --> J[写入模板内容]
    J --> K[显示成功信息]
    K --> L[显示后续步骤]
```

### 8.2 模板内容

```markdown
---
name: {skillName}
description: A brief description of what this skill does
---

# {skillName}

Instructions for the agent to follow when this skill is activated.

## When to use

Describe when this skill should be used.

## Instructions

1. First step
2. Second step
3. Additional steps as needed
```

## 9. 同步命令 (sync.ts)

### 9.1 同步流程

```mermaid
sequenceDiagram
    participant User
    participant CLI
    participant NodeModules
    participant LocalLock
    participant Installer

    User->>CLI: skills experimental_sync
    CLI->>NodeModules: discoverNodeModuleSkills()
    NodeModules-->>CLI: Skill[]

    CLI->>LocalLock: readLocalLock()
    LocalLock-->>CLI: LocalLockFile

    CLI->>CLI: 比较哈希值
    CLI->>CLI: 构建待安装列表

    CLI->>User: 选择目标代理
    User->>CLI: AgentType[]

    loop 每个技能
        loop 每个代理
            CLI->>Installer: installSkillForAgent()
            Installer-->>CLI: InstallResult
        end
    end

    CLI->>LocalLock: addSkillToLocalLock()
    CLI->>User: 显示结果
```

### 9.2 node_modules 发现

```typescript
async function discoverNodeModuleSkills(cwd: string) {
  const nodeModulesDir = join(cwd, 'node_modules');

  // 检查顶层包
  for (const name of topNames) {
    // 处理作用域包 @org/pkg
    if (name.startsWith('@')) {
      // 读取作用域下的所有包
    } else {
      // 处理普通包
    }

    // 检查 SKILL.md
    // 检查 skills/ 目录
    // 检查 .agents/skills/ 目录
  }
}
```

### 9.3 哈希比较

```typescript
for (const skill of discoveredSkills) {
  const existingEntry = localLock.skills[skill.name];
  if (existingEntry) {
    const currentHash = await computeSkillFolderHash(skill.path);
    if (currentHash === existingEntry.computedHash) {
      upToDate.push(skill.name);
      continue;
    }
  }
  toInstall.push(skill);
}
```

---

**下一篇**: [04-技能发现与解析](./04-技能发现与解析.md)
